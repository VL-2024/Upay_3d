import{CONFIG}from"./config.js";import{createScene}from"./scene.js";import{ScatterSystem}from"./scatter.js";import{updateAllOrientations}from"./orientation.js";import{validateLayout}from"./layout-validator.js";import{GameState,StateStore}from"./game-state.js";import{PairSelector}from"./pair-selector.js";import{InputController}from"./input.js";import{flickToTarget}from"./flick.js";import{DebugLabels}from"./debug-labels.js";const canvas=document.getElementById("renderCanvas"),engine=new BABYLON.Engine(canvas,true,{preserveDrawingBuffer:true,stencil:true,adaptToDeviceRatio:true}),havokInstance=await HavokPhysics(),{scene}=createScene(engine,canvas,havokInstance),store=new StateStore(),scatter=new ScatterSystem(scene),selector=new PairSelector(scene,store),labels=new DebugLabels(scene);let stableFrames=0,settlingStartedAt=0,rerollCount=0;new InputController(scene,canvas,store,selector,()=>scatter.pieces,(source,target)=>{selector.clearSelection();selector.updateVisuals(scatter.pieces);store.setState(GameState.FLICKING);flickToTarget(source,target);window.setTimeout(()=>{updateAllOrientations(scatter.pieces);labels.refresh(scatter.pieces,store.debug&&CONFIG.debug.labels);updatePairCount();store.setState(GameState.READY)},800)});function beginScatter(){selector.clearSelection();labels.refresh(scatter.pieces,false);store.setState(GameState.SCATTERING);scatter.scatter();stableFrames=0;settlingStartedAt=performance.now();store.setState(GameState.SETTLING);document.getElementById("hint").textContent="Чүкө рассыпаются…"}function reset(){selector.clearSelection();labels.refresh(scatter.pieces,false);scatter.clear();document.getElementById("pairCount").textContent="0";document.getElementById("selected").textContent="—";store.setState(GameState.INIT);document.getElementById("hint").textContent="Нажмите «РАССЫПАТЬ». Затем выберите чүкө и подсвеченную цель."}function allStable(){if(!scatter.pieces.length)return false;for(const p of scatter.pieces){const body=p.metadata.aggregate.body,lv=body.getLinearVelocity(),av=body.getAngularVelocity();if(lv.length()>CONFIG.physics.sleepLinearThreshold||av.length()>CONFIG.physics.sleepAngularThreshold)return false}return true}function finalizeLayout(){updateAllOrientations(scatter.pieces);const check=validateLayout(scatter.pieces);if(!check.valid&&rerollCount<CONFIG.layout.maxRerolls){rerollCount++;document.getElementById("hint").textContent=`Перерасклад ${rerollCount}/${CONFIG.layout.maxRerolls}…`;beginScatter();return}rerollCount=0;labels.refresh(scatter.pieces,store.debug&&CONFIG.debug.labels);selector.updateVisuals(scatter.pieces);updatePairCount();store.setState(GameState.READY);document.getElementById("hint").textContent=check.valid?"Выберите чүкө. Подходящие цели подсветятся.":"DEBUG: расклад принят после лимита reroll; можно тестировать щелчок."}function updatePairCount(){const check=validateLayout(scatter.pieces);document.getElementById("pairCount").textContent=String(check.pairCount)}document.getElementById("scatterBtn").addEventListener("click",beginScatter);document.getElementById("resetBtn").addEventListener("click",reset);document.getElementById("debugBtn").addEventListener("click",()=>{store.debug=!store.debug;document.getElementById("debugBtn").textContent=`DEBUG: ${store.debug?"ON":"OFF"}`;labels.refresh(scatter.pieces,store.debug&&CONFIG.debug.labels)});engine.runRenderLoop(()=>{document.getElementById("fps").textContent=engine.getFps().toFixed(0);if(store.state===GameState.SETTLING){if(allStable())stableFrames++;else stableFrames=0;const timedOut=performance.now()-settlingStartedAt>7000;if(stableFrames>=CONFIG.physics.stableFramesRequired||timedOut){finalizeLayout();stableFrames=0}}if(store.debug)labels.follow(scatter.pieces);scene.render()});window.addEventListener("resize",()=>engine.resize());