import{CONFIG}from"./config.js";export function validateLayout(pieces){const issues=[],halfW=CONFIG.field.width/2-CONFIG.field.safeMargin,halfD=CONFIG.field.depth/2-CONFIG.field.safeMargin;let closePairs=0;for(let i=0;i<pieces.length;i++){const a=pieces[i];if(Math.abs(a.position.x)>halfW||Math.abs(a.position.z)>halfD)issues.push(`${a.metadata.id}: OUTSIDE`);if(a.position.y>1.5)issues.push(`${a.metadata.id}: STACK/HIGH`);for(let j=i+1;j<pieces.length;j++){const b=pieces[j],d=BABYLON.Vector3.Distance(a.position,b.position);if(d<CONFIG.layout.minDistance)closePairs++}}const pairCount=countMatchingStatePairs(pieces.filter(p=>!p.metadata.isKhan));if(pairCount<CONFIG.layout.requiredPairCount)issues.push(`PAIR_COUNT=${pairCount}`);if(closePairs>CONFIG.layout.maxOverlapsAllowed)issues.push(`CLOSE=${closePairs}`);const khan=pieces.find(p=>p.metadata.isKhan);if(!khan)issues.push("NO_KHAN");return{valid:issues.length===0,issues,pairCount,closePairs}}export function countMatchingStatePairs(pieces){const counts=new Map();for(const p of pieces){const s=p.metadata.state;counts.set(s,(counts.get(s)||0)+1)}let pairs=0;for(const n of counts.values())pairs+=Math.floor(n/2);return pairs}